{% comment %}
  Color Scheme Snippet
  Outputs the CSS class for a color scheme from the color_scheme_group settings

  Usage:
  {% render 'color-scheme', scheme: section.settings.color_scheme %}

  Or inline as a class:
  <div class="{% render 'color-scheme', scheme: section.settings.color_scheme %}">

  The color_scheme setting type returns a color_scheme object which has an id property.
  This snippet outputs the class name in the format: color-{scheme_id}
{% endcomment %}

{%- if scheme != blank -%}color-{{ scheme }}{%- endif -%}
