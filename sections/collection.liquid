{% comment %}
  This section is used in the collection template to render collection page
  listing all products within a collection.

  https://shopify.dev/docs/storefronts/themes/architecture/templates/collection
{% endcomment %}

<section class="collection-hero full-width" style="background: var(--green900); padding: 3rem 0 2rem 0;">
  <div class="collection-hero__content page-width">
    <h1
      class="collection-title"
      style="font-family: var(--heading-font); color: var(--grey); font-size: 2.5rem; text-align: center; margin-bottom: 1rem;"
    >
      Welcome to Your Peaceful Garden
    </h1>
    <p
      class="collection-intro"
      style="font-family: var(--body-font); color: var(--grey); font-size: 1.15rem; text-align: center; max-width: 700px; margin: 0 auto 2rem auto;"
    >
      At Andra's Garden Heaven, we believe your garden should be a sanctuary—a place to unwind, connect, and find peace
      in everyday moments. Our handcrafted furniture is made with care and passion, designed to bring comfort, beauty,
      and a sense of calm to your outdoor space. Whether you're sharing laughter with friends or enjoying a quiet cup of
      tea, our pieces are here to help you create memories and embrace the joy of simple living. Welcome home to peace,
      comfort, and community.
    </p>
  </div>
</section>

<section class="collection-products-section" style="background: var(--white); padding-bottom: 4rem;">
  <div class="collection-products-grid page-width">
    {% paginate collection.products by 20 %}
      {% for product in collection.products %}
        <div class="product-card">
          <a href="{{ product.url }}" class="product-card__link">
            <div class="product-card__image-card">
              {% assign main_image = product.featured_image | default: product.images.first %}
              {% if main_image %}
                {% render 'image',
                  class: 'product-card__image',
                  image: main_image,
                  url: product.url,
                  width: 400,
                  height: 400,
                  crop: 'center'
                %}
              {% else %}
                <div
                  class="product-card__placeholder"
                  style="background: var(--grey); width: 100%; height: 0; padding-bottom: 100%; border-radius: 1rem;"
                ></div>
              {% endif %}
            </div>
            <div class="product-card__content">
              <h2 class="product-card__title">
                {{ product.title | escape }}
              </h2>
              <p class="product-card__price">
                {{ product.price | money }}
              </p>
            </div>
          </a>
        </div>
      {% endfor %}

      {% if paginate.pages > 1 %}
        <div class="pagination-wrapper" style="grid-column: 1 / -1; text-align: center; margin-top: 3rem;">
          {{ paginate | default_pagination }}
        </div>
      {% endif %}
    {% endpaginate %}
  </div>
</section>

<style>
  :root {
    --green: #1c4219;
    --grey: #d0ccd0;
    --white: #fbfcff;
    --blue: #1c6e8c;
    --black: #000;
    --heading-font: 'Futura Book', sans-serif;
    --body-font: 'Elgian', serif;
    --border-radius: 1rem;
  }
  .page-width {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }
  .collection-hero {
    background: var(--green900, var(--green));
    padding: 3rem 0 2rem 0;
  }
  .collection-title {
    font-family: var(--heading-font);
    color: var(--grey);
    font-size: 2.5rem;
    text-align: center;
    margin-bottom: 1rem;
  }
  .collection-intro {
    font-family: var(--body-font);
    color: var(--grey);
    font-size: 1.15rem;
    text-align: center;
    max-width: 700px;
    margin: 0 auto 2rem auto;
  }
  .collection-products-section {
    background: var(--white);
    padding-bottom: 4rem;
  }
  .collection-products-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }
  .product-card {
    background: var(--green900, var(--green));
    color: var(--grey);
    border-radius: var(--border-radius);
    box-shadow: 0 4px 24px rgba(28, 66, 25, 0.08);
    overflow: hidden;
    transition: box-shadow 0.2s, transform 0.2s;
    min-height: 420px;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    justify-content: flex-start;
  }
  .product-card:hover {
    box-shadow: 0 8px 32px rgba(28, 66, 25, 0.16);
    transform: translateY(-4px) scale(1.02);
    text-decoration: underline var(--blue);
  }
  .product-card__link {
    text-decoration: none;
    display: flex;
    flex-direction: column;
    height: 100%;
  }
  .product-card__image-card {
    background: var(--white);
    border-radius: var(--border-radius) var(--border-radius) 0 0;
    box-shadow: 0 2px 12px rgba(28, 66, 25, 0.1);
    padding: 1.25rem 1.25rem 0.5rem 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 220px;
    max-height: 320px;
    overflow: hidden;
  }
  .product-card__image {
    width: 100%;
    height: auto;
    object-fit: cover;
    border-radius: 0.75rem;
    box-shadow: 0 1px 6px rgba(28, 66, 25, 0.06);
    background: var(--white);
  }
  .product-card__content {
    padding: 1.5rem 1.25rem 1.25rem 1.25rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    background: transparent;
  }
  .product-card__title {
    font-family: var(--heading-font);
    color: var(--grey);
    font-size: 1.25rem;
    margin: 0 0 0.5rem 0;
    font-weight: 600;
    letter-spacing: 0.01em;
  }
  .product-card__price {
    font-family: var(--body-font);
    color: var(--grey);
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0;
  }
  .product-card__placeholder {
    width: 100%;
    height: 0;
    padding-bottom: 100%;
    border-radius: var(--border-radius);
    background: var(--grey);
  }
  .pagination-wrapper {
    grid-column: 1 / -1;
    text-align: center;
    margin-top: 3rem;
  }
  @media (max-width: 768px) {
    .collection-hero {
      padding: 2rem 0 1rem 0;
    }
    .collection-title {
      font-size: 2rem;
    }
    .collection-products-grid {
      gap: 1.25rem;
    }
    .product-card__content {
      padding: 1.25rem 1rem;
    }
    .product-card__image-card {
      padding: 1rem 1rem 0.5rem 1rem;
    }
  }
  @media (max-width: 480px) {
    .collection-title {
      font-size: 1.5rem;
    }
    .collection-intro {
      font-size: 1rem;
    }
    .collection-products-grid {
      grid-template-columns: 1fr;
    }
    .product-card__content {
      padding: 1rem 0.75rem;
    }
    .product-card__image-card {
      padding: 0.75rem 0.75rem 0.5rem 0.75rem;
    }
  }
</style>

{% schema %}
{
  "name": "t:general.collection",
  "settings": []
}
{% endschema %}
